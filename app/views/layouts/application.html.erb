<!DOCTYPE html>
<html>
  <head>
    <title>DemoApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="container">
      <nav class="mt-3 mb-5 navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
          <a class="navbar-brand" href="#">Rails</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/post_list">Post List</a>
              </li>
              <li class="nav-item">
                <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
              </li>
            </ul>
          </div>
        </div>
      </nav>

      <%= yield %>


      <% acc = DevTools::Acc.dump %>


      <%= DevTools::Acc.to_json %>

      <table class="mt-5 table table-hover">
        <thead>
        <tr class="table-dark">
          <th scope="col">Type</th>
          <th scope="col">Description (ID: <%= acc[:id] %>)</th>
        </tr>
        </thead>

        <tbody>

        <tr class="table-primary">
          <td>
            Summary
          </td>
          <td>
            Models: <%= acc[:models].values.sum %>
            <br>
            Queries: <%= acc[:queries].size %> (<%= acc[:queries].map{ _1[:duration]}.sum.round(1) %>ms)
          </td>
        </tr>

        <tr>
          <td>
            Models
          </td>
          <td>
            <% acc[:models].each do |m, c| %>
              <%= m %>: <strong><%= c %></strong> <br>
            <% end %>
          </td>
        </tr>

        <% acc[:queries].each do |q| %>
          <tr>
            <td>SQL</td>
            <td>
              <strong><%= q[:name] %></strong> <br>
              <%= q[:sql] %> <br>
              <small> ↳ <%= q[:source].join(" ") %></small>
            </td>
          </tr>
        <% end %>

        </tbody>
      </table>
    </div>
  </body>
</html>
